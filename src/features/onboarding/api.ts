/** Onboarding API — re-exported from the settings module, which owns the
 * profile/settings/onboarding endpoints as one unit. */

export {
  completeOnboarding,
  fetchOnboardingContext,
  type OnboardingContext,
} from '@/features/settings/api'
